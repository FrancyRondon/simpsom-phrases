import styled from "styled-components";


export default function Header(params) {
    return(
        <Title>Las mejores frases de Los Simpsom</Title>
    )
};

const Title = styled.h1`
        color: #e76f51;
        text-align: center;
`;